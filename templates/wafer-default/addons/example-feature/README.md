# example-feature add-on

Adds a database-backed example feature to `wafer-default` using todos as the concrete pattern.

Install:

    bun run add db
    bun run add example-feature
    bun install

Start the app and open `/todos`. The API lives at `/api/todos`. Treat this as example wiring for agents to copy, not as a product feature every app should keep.
